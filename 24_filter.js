// 24 - filter
// Selects elements that satisfy a condition. Returns a new array. 
const numbers = [1, 2, 3, 4, 5, 6]; 

const evenNumbers = numbers.filter(num => num % 2 === 0); 
console.log(evenNumbers); 